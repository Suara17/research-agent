我想使用 `e:\Research_Agent\DeepResearchAgent-main` 这个项目来完成复杂多跳问题的搜索任务了，需要修改使得其符合要求，有些已有模块可以使用 `e:\Research_Agent\research_agent` 里面的。赛制要求： `e:\Research_Agent\docs\算法大赛-天池大赛-阿里云的赛制.md`
### DeepResearchAgent-main 改造计划方案

基于对 `research_agent`（参考项目）和 `DeepResearchAgent-main`（目标项目）的深度分析，结合天池大赛的赛制要求，制定以下改造方案。本方案旨在保留 `DeepResearchAgent` 强大的规划与反思能力，同时融合 `research_agent` 优秀的搜索工具和符合赛题的工程封装。

#### 1. 总体架构设计

改造后的项目将包含三层架构：
*   **服务层 (Service Layer)**: 参考 `research_agent/agent.py`，使用 FastAPI 封装标准 HTTP 接口，负责请求接收、Agent 调度、结果清洗与响应。
*   **Agent 核心层 (Core Layer)**: 使用 `DeepResearchAgent` 的多智能体框架 (Planner + Actor)，通过配置适配阿里云百炼 Qwen 模型和iflow调用的qwen模型。
*   **工具层 (Tool Layer)**: 移植 `research_agent` 的 `search.py` 和 `intelligent_fetcher.py`，替换原有的简易搜索工具，提供多源搜索和深度网页解析能力。

---

#### 2. 详细实施步骤

##### 2.1 模型适配 (Model Configuration)
**参考**: `research_agent/utils.py` 中的 `get_llm_client`
**目标**: 让 `DeepResearchAgent` 使用阿里云百炼 API。

*   **修改点**: 主要是配置文件 `configs/config_main.py`。
*   **方案**:
    *   将 `model` 配置部分的 `api_base` 指向阿里云百炼兼容端点 `https://dashscope.aliyuncs.com/compatible-mode/v1`。
    *   `model_id` 设置为 `qwen-max` 或 `qwen-plus`。
    *   `api_key` 从环境变量 `DASHSCOPE_API_KEY` 获取。
    *   保留 `OpenAIServerModel` 类 (`src/models/openaillm.py`) 不变，它天然支持 OpenAI 兼容接口。
也移植@agent.py目前使用的iflow模型作为暂时首选模型
##### 2.2 工具移植与增强 (Tool Migration)
**参考**: `research_agent/search.py` (web_search, web_fetch)
**目标**: 赋予 Agent 强大的“眼睛”和“手”。

*   **新建工具文件**: 在 `src/tools/` 下创建 `deep_search_tools.py`。
*   **逻辑移植**:
    *   **Search**: 移植 `web_search` 函数，保留 Serper/Bocha/DDGS 多源自动切换逻辑。
    *   **Fetch**: 移植 `web_fetch` 函数，保留 Jina Reader -> Trafilatura -> Requests 的降级策略，以及 PDF 解析能力。
*   **封装适配**:
    *   使用 `smolagents` 的 `@tool` 装饰器或继承 `AsyncTool` 类，将上述函数封装为 `DeepSearchTool` 和 `DeepFetchTool`。
    *   确保工具的 `docstring` 清晰，以便 Planner 准确理解用途。

##### 2.3 API 服务封装 (Service Encapsulation)
**参考**: `research_agent/agent.py`
**目标**: 符合赛题 EAS 部署标准 (POST /, JSON in/out)。

*   **新建入口文件**: 在项目根目录创建 `app.py`。
*   **实现逻辑**:
    1.  **初始化**: 在应用启动时（`startup` 事件）初始化 `DeepResearcherAgent`，加载 Config 和 Prompt，避免每次请求冷启动。
    2.  **请求处理 (`/`)**:
        *   接收 `{"question": "..."}`。
        *   调用 `agent.run(task=question)`。
        *   **关键点**: `DeepResearchAgent` 通常返回包含思考过程的 Markdown，需要进行清洗。
    3.  **结果清洗**:
        *   移植 `research_agent/utils.py` 中的 `clean_answer` 函数。
        *   在返回 JSON 前，调用 `clean_answer(agent_result)` 提取纯文本答案。

##### 2.4 依赖管理 (Dependency Management)
**参考**: `research_agent` 和 `DeepResearchAgent-main` 的 `requirements.txt`
**目标**: 确保环境兼容且不臃肿。

*   **合并依赖**:
    *   保留 `DeepResearchAgent` 的核心: `smolagents`, `rich`, `pydantic`。
    *   引入 `research_agent` 的工具依赖: `fastapi`, `uvicorn`, `requests`, `beautifulsoup4`, `trafilatura`, `pypdf`, `tenacity`。
    *   **移除**: `browser-use`, `playwright`, `crawl4ai` 等重型浏览器依赖（赛题环境可能不支持，且 `web_fetch` 已足够强大）。

---

#### 3. 核心优势分析

通过这种组合，我们将获得：
1.  **DeepResearchAgent 的脑**: 强大的 Step-by-Step 规划能力，能够处理长难句和多跳逻辑（解决 `qid=7` 问题的关键）。
2.  **Research_Agent 的手**: 经过验证的稳定搜索和解析工具，能够处理 PDF、抗反爬、多源互备。
3.  **标准工程外壳**: 符合赛制的 FastAPI 接口和答案清洗逻辑。
