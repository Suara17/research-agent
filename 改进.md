
### 现状分析

1.  **现有搜索实现**：
    *   目前 `research_agent/search.py` 中的 `web_search` 函数已经支持 `Serper`、`Bocha`、`SerpApi` (通过 `requests` 调用) 和 `DuckDuckGo`。
    *   代码中缺乏对百度搜索（Baidu）的原生支持，这对于中文语境下的搜索是一个缺失。
    *   Google 搜索目前依赖 API Key，缺乏无 Key 的 fallback 方案（如 `googlesearch-python`）。

2.  **依赖情况**：
    *   根目录下的 `requirements.txt` 缺少您提到的四个依赖包。
    *   `DeepResearchAgent-main/requirements.txt` 中虽然包含这些依赖，但似乎未在主逻辑中完全启用。

### 集成方案

为了集成更多搜索源并增强搜索能力的鲁棒性，建议按以下步骤操作：

#### 1. 添加依赖

首先，我们需要在项目根目录的 `requirements.txt` 中添加指定的依赖库：

```text
serpapi>=0.1.5
google-search-results>=2.4.2
baidusearch>=1.0.3
googlesearch-python>=1.3.0
```

#### 2. 修改 `research_agent/search.py`

我们需要在 `search.py` 中引入这些库，并在 `web_search` 函数中添加相应的调用逻辑。

**Step 2.1: 引入库**
在文件头部添加 try-except 导入块，防止因缺少依赖导致整个程序崩溃：

```python
try:
    from serpapi import GoogleSearch
    _SERPAPI_LIB_AVAILABLE = True
except ImportError:
    _SERPAPI_LIB_AVAILABLE = False

try:
    from baidusearch.baidusearch import search as baidu_search
    _BAIDU_AVAILABLE = True
except ImportError:
    _BAIDU_AVAILABLE = False

try:
    from googlesearch import search as google_search_scraper
    _GOOGLE_SCRAPER_AVAILABLE = True
except ImportError:
    _GOOGLE_SCRAPER_AVAILABLE = False
```

**Step 2.2: 集成逻辑 (在 `web_search` 函数中)**

*   **Baidu Search (针对中文查询优化)**：
    在 `is_chinese_query` 判断为 True 时，优先尝试调用 `baidusearch`。

*   **SerpApi (官方库集成)**：
    优化现有的 `serpapi` 逻辑。如果检测到 `_SERPAPI_LIB_AVAILABLE` 为 True，则使用官方 `GoogleSearch` 类进行调用，这通常比手动构造 `requests` 请求更稳定。

*   **Google Scraper**：
    在 API Key 方式：Serper不能使用时，就使用 `googlesearch-python` 第二方案。注意需要限制频率以避免被封锁。

**代码集成示意图：**

```python
# 在 web_search 循环中...

# 1. 尝试 Baidu (如果是中文查询且库可用)
if is_chinese_query and _BAIDU_AVAILABLE:
    # 调用 baidu_search 逻辑...
    pass

# ... 现有的 Serper / Bocha 逻辑 ...

# 2. 优化 SerpApi 调用
if serpapi_key:
    if _SERPAPI_LIB_AVAILABLE:
        # 使用 GoogleSearch(params) 官方客户端调用
        pass
    else:
        # 保留原有的 requests 调用作为降级
        pass

# ... 现有的 Brave / DDGS 逻辑 ...

```

### 总结

这个方案将在不破坏现有逻辑的基础上，显著增强 Agent 的搜索广度和适应性，特别是对中文内容的覆盖（通过 Baidu）和网络搜索可用性（通过 Scraper）。
